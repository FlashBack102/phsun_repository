<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	    
<mapper namespace="com.example.start.guest.persistence.GuestDAO">
	<insert id="register" parameterType="java.util.Map">
		INSERT INTO member(id, pwd, authority)
		VALUES (#{id}, #{pwd}, #{authority})
	</insert>
	
	<select id="login" resultType="java.util.Map">
		SELECT id as username,
			   pwd as password,
			   enabled,
			   authority  
		  FROM member
		 WHERE id=#{username}
	</select>

</mapper>